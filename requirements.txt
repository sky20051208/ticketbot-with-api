# 搶票機器人套件清單
# Python 3.10 ~ 3.12（curl_cffi 尚未支援 3.13）

# Web GUI（網頁版 War-Room）
fastapi>=0.110
uvicorn[standard]>=0.27

# 搶票核心（curl_cffi 打拓元 API；selenium 給拓元 userdata cookie 登入 + 搶到後開結帳頁）
curl_cffi>=0.7
selenium>=4.18

# nodriver：KKTIX / 寬宏 / TicketPlus 開瀏覽器登入 + 頁面內 fetch，遠端登入閘道（CDP screencast）
nodriver>=0.40

# TicketPlus：網址上的活動 id 是 AES-128-CBC 加密的明文 id，解密要用（selenium 也依賴它）
cryptography>=42.0

# 驗證碼辨識（拓元：自訓 CRNN+CTC ONNX，~99% acc）
onnxruntime>=1.16
numpy>=1.24
Pillow>=10.0

# 寬宏驗證碼：ddddocr + 限制字元集（現場實測 96.8%，舊的自訓 ONNX 只有 80%）。
# 鎖版本：kham_api/captcha.py 自己解 probability 輸出，1.6 的介面沒驗證過
ddddocr==1.5.6

# 對時 + HTTP
requests>=2.31
ntplib>=0.4

# 測試（tester subagent / 開發用；正式搶票不需要）
pytest>=8.0
